node_modules/
.env
*.log
*.log.*
dist/

# Perfil de Chrome dedicado a stephy (ruido recurrente en git status; no versionar)
chrome-user-data/

# Estado de la búsqueda incremental (#4): trabajo local con backoff por tracking.
data/search-state.json

# Salud de corridas (#11): historial local para alertar rachas de fallos.
data/run-health.json

# Artefactos de corrida (#12): histórico por corrida + archivo de trabajo de n8n.
# Son datos locales que se regeneran cada corrida; no versionar (ruido en git status).
data/history/
data/nops-con-tracking.json

# Salidas de la consulta de consignatarios: llevan nombres de clientes y números
# de consignatario. Son datos de la corrida, se regeneran; NO versionar.
data/consignee-*.json
data/consignee-*.csv
data/receipts-*.csv
data/receipts-contaminados.json

# Claude Code personal/local settings (no versionar)
.claude/settings.local.json
